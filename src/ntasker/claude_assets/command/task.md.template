---
description: Load an nTasker task into the session for processing
argument-hint: <task-id> | #<task-id>
allowed-tools: Bash(python3 {HELPER_PATH}:*)
---

# Task #$ARGUMENTS from nTasker (via /{COMMAND_NAME})

!`python3 {HELPER_PATH} "$ARGUMENTS"`

---

## Instructions

You have been handed task #$ARGUMENTS from nTasker.

1. **Read the task data above** (title, description, project, tags, phase, priority).
2. **If a `Project` is assigned** and you know where its code lives on this machine, switch to that working directory before making changes.
3. **On unclear requirements:** ask, do not assume.
4. **When you believe your work is done -- hand it off to review:**
   - Post a compact status summary in chat (what changed, key files, test status).
   - Move the task to `phase=review` so it shows up in the user's
     "Zu prüfen" / Review column for verification. This is automatic --
     no user OK required, because review is a *handoff*, not a *close*:
     ```bash
     ntasker patch "$ARGUMENTS" --phase review
     # or:
     curl -s -X PATCH "http://127.0.0.1:8766/api/tasks/$ARGUMENTS" \
       -H 'Content-Type: application/json' \
       -d '{"phase":"review"}'
     ```
   - **The user, not you, flips the task to `status: done`.** Do not
     send `{"status":"done"}` even after the review is finished. The
     user closes the task themselves via the kanban Done column or the
     list checkbox.
   - If you could not finish (blocker, missing info, failed
     verification), do NOT move to review -- leave the phase as-is and
     report the blocker.

**Hard rules:**

- **Never mark `status: done` autonomously** -- `status=done` is a user
  decision, period. Ask the user only if they explicitly want to flip
  status themselves; the agent never sends `{"status": "done"}`.
- The only autonomous tracker write allowed for #$ARGUMENTS is
  `phase: review`, and only when you genuinely finished your part.
- Archiving stays a manual decision -- never set `archived: true` on
  someone else's behalf.
- No new tasks, no deletes, no writes to *other* task IDs.

Start with step 1.
